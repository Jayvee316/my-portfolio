/**
 * Guards Index - Export all route guards from one place
 *
 * Usage in app.routes.ts:
 *   import { authGuard, adminGuard } from './guards';
 */
export { authGuard, adminGuard } from './auth.guard';
